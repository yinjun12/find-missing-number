    int missingNumber(vector<int>& nums) {
        vector<int>vec(nums.size()+1,-1);
        for(int i=0;i<nums.size();i++)
        vec[nums[i]]=0;
        int i=0;
        
        for(;i<vec.size();i++)
        if(vec[i]==-1) break;
        return i;
        
    }
